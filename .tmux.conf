# Set prefix
unbind C-b
set -g prefix C-a
bind C-a send-prefix

set-window-option -g utf8 on
set -g default-terminal "screen-256color"
set -g history-limit 10000

if-shell "test -f .tmuxstatusbar" "source .tmuxstatusbar"

set -g mouse on
